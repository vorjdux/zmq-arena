# Independent toolchain pin for this target. A target may need a different
# channel than the orchestrator (e.g. a newer edition). The orchestrator's pin
# does not apply here because this is not a workspace member.
[toolchain]
channel = "1.78.0"
profile = "minimal"
