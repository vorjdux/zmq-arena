# Independent toolchain pin for this target (zmq.rs / the `zeromq` crate).
# Bumped to a recent stable so the engine's dependency closure builds; the root
# orchestrator pin does not apply (not a workspace member).
[toolchain]
channel = "1.96.0"
profile = "minimal"
