# Build artifacts. Each project has its own target/ dir.
/target
**/target
# Engines that pick their runtime at compile time are built once per runtime,
# each into its own target dir, because every shipped runtime is a measured
# variant.
**/target-tokio
**/target-smol
**/target-async-std
**/target-async-dispatcher

# C++ target build artifacts
targets/libzmq_cpp_target/build/
*.o
*.obj

# Python bytecode. The scripts/ helpers are run directly, so CPython drops a
# __pycache__ next to them on every invocation. These were tracked by mistake,
# which meant merely rendering a run or regenerating the matrix dirtied the
# working tree and produced meaningless binary diffs in review.
__pycache__/
*.py[cod]

# Local run scratch (raw telemetry before archival)
/scratch/
*.hgrm
*.profraw

# NOTE: docs/history/ is deliberately NOT ignored. Run archives are committed
# like any other output. They used to be ignored to keep dev-host numbers out of
# the repo, but that left no path at all from a run to the published dashboard:
# CI deploys from a fresh checkout, so an archive that is not in git is invisible
# to it. A run that should not be published should not be rendered, and every
# archive records the host it came from either way.

# Cargo.lock is intentionally NOT ignored. Every project here is a binary, and
# the arena requires deterministic builds: the orchestrator and each standalone
# target commit their lockfiles so a grid run is reproducible.
